package has_a.association

import has_a.parent_class.HalterKT

class HorseKT(halterKT: HalterKT)


/**
 * Ассоциация - это отношение,
 * когда объекты классов
 * взаимодействуют друг с другом,
 * но каждый остается независимым.
 * Просто говоря, это "имеет"
 * отношение.
 * */