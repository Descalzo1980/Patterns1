package has_a.aggregation

import has_a.parent_class.HalterKT

/*class Horse {
    val halter: HalterKT? = null
}*/

/**
 * Агрегация - это вид ассоциации,
 * когда один объект содержит ссылку
 * на другой объект, но тот объект
 * может существовать и независимо
 * от первого объекта. Это "имеет"
 * отношение, но без жесткой
 * зависимости.
 * */